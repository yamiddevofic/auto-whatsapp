[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm ci",
  "cd server && npm ci",
  "cd client && npm ci"
]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "node server/src/index.js"
